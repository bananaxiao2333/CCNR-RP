# CCNR-RP 界面主题设计 · 黑白灰军用终端（docs/14）

> 目标：把 in-game 界面视觉统一为「黑白灰军用终端」，与 Web 管理面板（ccnr-rp-gui / src/admin）保持一致。
> **主题 v4** 替换原 **v3「CCNR:NET 机密终端」**（冷暗科技金属底 / 青色主色 / 金·蓝徽章 / 高饱和正红选中）。
> **主题 v4.1（2.24.1）** 是 v4 的**令牌收口**：设计语言、调色板与布局不变，把散落在各界面里的裸色值
> 登记为语义令牌并抽出共享绘制入口，使「同类构件只有一种画法」（见 §2.1 / §5.4）。
> 本设计只改「视觉层」（颜色/描边/选中态/文字层级/容器装饰），**不触碰**布局、几何、行为、配置、网络、存档与运行时逻辑。

## 1. 设计语言（一句话）

> 近黑冷暗底 / 白·灰等宽文字 / 细灰边 / **白 = 强调**（选中反白）/ **红 = 警示危险** / **灰阶徽章**（机构等级）。
> 除红色警示外全程无彩色，仅以明度（白 > 亮灰 > 暗灰）划分层级。

- 底色：近黑（#000 ~ #141414），面板灰阶底、深色卡片。
- 文字：白（主）/ 亮灰（次）/ 暗灰（弱），等宽字体（MC 字体近似）。
- 边框：细灰（#333 / #666），四角 L 型角标。
- 强调：白色（按钮主操作、选中反白、游标、徽章亮阶）。
- 危险：正红（#FF3B30 / #FF4A40），唯一保留彩色。

## 2. 设计令牌（RpTheme.java 常量，v4）

| 令牌 | ARGB | 说明 |
| --- | --- | --- |
| OVERLAY | 0xB3141414 | 面板半透明底（近黑） |
| BG_DEEP | 0xCC0A0A0A | 更深背景 |
| PANEL_BG / _ALT / _EVEN | 0x99141414 / 0xB3222222 / 0x9B161616 | 卡片 / 列表底 |
| PANEL_BORDER | 0xFF333333 | 细灰边 |
| PANEL_BORDER_BRIGHT | 0xFF666666 | 亮灰边 |
| TEXT_PRIMARY | 0xFFFFFFFF | 主文字（白） |
| TEXT_SECONDARY | 0xFFA0A0A0 | 次文字 |
| TEXT_DIM | 0xFF666666 | 弱文字 |
| CYAN（= 主色） | 0xFFFFFFFF | 白色强调 |
| CYAN_DIM | 0xFF888888 | 灰强调 |
| GREEN（= 正面/正常） | 0xFFB4B4B4 | 中性亮灰 |
| RED / RED_DIM / RED_LINE | 0xFFFF3B30 / 0xFF8C2320 / 0xFFFF4A40 | 危险 / 警戒 |
| GOLD（= 徽章亮阶） | 0xFFB4B4B4 | 灰阶徽章 |
| STATUS_ALIVE / _DEAD / _OBSERVING | 白 / 红 / 灰 | 状态色 |

**等级徽章灰阶**（tierColor）：
- 1 级（最高机密）→ 白（TEXT_PRIMARY）
- 2 级（标准机构）→ 亮灰（TEXT_SECONDARY）
- 3 级（普通编制）→ 暗灰（TEXT_DIM）

## 2.1 令牌全表（v4.1 补全，2.24.1）

> v4 只登记了「面板/文字/主色/危险」主干令牌，进阶浮层、控件、遮罩、列表、滚动条、徽章槽位、立体预览全息层
> 仍是各界面裸色值。v4.1 把它们全部收口（值保持原样，仅登记语义名，避免视觉漂移）：

| 组 | 令牌 | 值 | 用途 |
| --- | --- | --- | --- |
| 底板 | SURFACE_SCREEN | 0xEE161616 | 全屏界面实心底（关系图） |
| | SURFACE_CARD / _DIM | 0xEE1A1A1A / 0xEE3A3A3A | 浮层·HUD 卡片 / 次级·未激活卡片 |
| | SURFACE_ALERT | 0xEE2E2E2E | 警示浮层卡片底（配红系边） |
| | SURFACE_POPUP | 0xE0323232 | 下拉 / 悬浮浮层底 |
| | SURFACE_CONTROL / _HOVER | 0xA8323232 / 0xD03A3A3A | 控件常态 / 悬停 |
| | SURFACE_CONTROL_ON / _OFF | 0xCC3F3F3F / 0xCC323232 | 开关·分段控件 开 / 关 |
| | SURFACE_INSET | 0xB0383838 | 内陷槽（进度条 / 只读域） |
| | SURFACE_SUNKEN | 0xFF202020 | 实心沉底（立体预览底盘） |
| | SURFACE_DISC | 0xA8141414 | 关系图文本节点底盘（原 v3 蓝黑残留的归一值） |
| 边框 | BORDER_DIM | 0xFF4A4A4A | 弱化边（空装备槽 / 锁定项） |
| 遮罩 | SCRIM / SCRIM_LIGHT | 0xA6000000 / 0x99000000 | 模态遮罩 / 轻遮罩 |
| | VEIL / SHADOW | 0x32000000 / 0x66000000 | 变暗蒙层 / 条底阴影 |
| | TRANSPARENT / BLACK | 0x00000000 / 0xFF000000 | 占位透明 / 纯黑 |
| 列表 | ROW_STRIPE | 0x1FFFFFFF | 浮层内细亮条纹 |
| | ROW_SEL / _HOVER / ROW_IDLE | 0xAA313131 / 0xAA3A3A3A / 0x99333333 | 分段控件 选中 / 悬停 / 常态 |
| 滚动条 | SCROLL_TRACK / _ACTIVE | 0x40383838 / 0x80383838 | 无溢出 / 可滚动 |
| | TAB_TRACK | 0x24FFFFFF | 页签横向滚动轨道 |
| 徽章槽位 | BADGE_DISC / BADGE_PUNCH | 0xFF2E2E2E / 0xFF161616 | 徽章底盘 / 图形挖空 |
| | SLOT_BG / SLOT_PUNCH | 0xFF2F2F2F / 0xE62F2F2F | 装备槽底 / 槽内挖空 |
| 全息层 | PREVIEW_BEAM_TOP / _BEAM | 0x00FFFFFF / 0x4DFFFFFF | 立体预览光柱 顶 / 底 |
| | PREVIEW_RING / _GRID | 0x8CFFFFFF / 0x5AFFFFFF | 底座发光环 / 地格线 |
| 文字 | TEXT_BRIGHT | 0xFFB4B4B4 | 亮灰强调（正数 / 提示）；与 GREEN 同值——单色终端里「正面」即亮灰 |
| | TEXT_DISABLED | 0xFF6A6A6A | 禁用文字 |
| 主操作 | ACCENT_FILL / _HOVER | 0xFFFFFFFF / 0xFFDADADA | 主按钮填充 / 悬停填充 |
| 危险 | RED_BG_SOFT / _HOVER / _FILL | 0xAE150C0C / 0xD0641613 / 0xE66F1613 | 危险按钮 常态 / 悬停 / 实心块 |
| 关系 | NEUTRAL / HOSTILE / FRIENDLY | 白 / RED / 0xFF4FA6FF | 关系语义三态（`relationColor(String)` 统一取值） |
| 状态 | STATUS_ALIVE / _DEAD / _OBSERVING | 白 / 0xFFFF3B30 / 0xFF7E8A8F | 三态状态色（`statusColor(String)`，未知/null → 观察态）；观察态为刻意保留的弱灰青 |
| 屏幕质感 | SCANLINE / SHEEN | 0x05FFFFFF | 每 3px 微亮扫描线 / 顶栏光泽 |
| 兼容别名 | ACCENT / ACCENT_HOVER / ACCENT_TEXT | = CYAN_DIM / CYAN / 0xFF111111 | 旧「按钮边界色」命名；`ACCENT_TEXT` 即反白墨字（语义见 §6 硬约束） |
| | ACCENT_FILL / _HOVER | = CYAN / 0xFFDADADA | 主操作填充 / 悬停填充 |
| | DANGER / DANGER_HOVER | = RED_DIM / RED | 危险按钮兼容名 |
| | BLUE_BADGE / COOLDOWN | = CYAN_DIM / TEXT_BRIGHT | 徽章蓝 / 冷却灰兼容名（单色终端里均已中性化） |

**共享绘制入口**（同类构件只能走这些入口，禁止再写 `g.fill` + 裸色值）：

| 入口 | 用途 |
| --- | --- |
| `listPanel` / `listRow` / `listHeaderRule` | 列表容器 / 行（斑马纹+悬停，含鼠标命中重载）/ 表头分隔线 |
| `sectionCard` / `card` / `terminalPanel` / `terminalFrame` | 分组卡片 / 内嵌卡片 / 素版面板 / 顶层面板（角标+rail） |
| `hudCard`（可指定底与边）/ `accentBar` | 浮层·HUD 卡片 / 左侧 3px 状态条 |
| `controlBox` | 输入与下拉控件框（常态/激活描边） |
| `popupPanel` / `popupRow` / `popupRowText` | 下拉与悬浮浮层 / 候选行底 / 候选行文字色 |
| `suggestionPopup` / `suggestionRow` | 输入补全浮层 / 补全候选行（键盘当前项反白 + 悬停高亮） |
| `modalScrim` / `lightScrim` | 模态遮罩 / 轻遮罩 |
| `wrapText`（按像素逐字符贪心断行，含纯逻辑重载）/ `clip`（按像素裁剪 + 省略号） | 多行文本与单行长文本的唯一入口：弹窗正文/字幕/简历走 `wrapText`，列表行与弹窗里的 id/名称走 `clip`。2.25.2 起 client 包内 6 份同构实现（`wrapText`×2 + `clip`×4）全部转调此处——**按空格断行对中文无效**（整句不换行、溢出弹窗），已由 `RpThemeTextTest` 钉死 |
| `selectedBar` / `scanlines` / `gridOverlay` / `cornerBrackets` / `tag` / `section` | 选中反白 / 扫描线 / 网格 / 角标 / `[ 标签 ]` / `// 分区` |

## 3. 与前端映射（ccnr-rp-gui App.css / theme.js）

| 前端令牌 | 值 | 模组映射 |
| --- | --- | --- |
| --bg / --bg-secondary | #000 / #0a0a0a | 近黑底 |
| --border / --border-active | #1a1a1a / #333 | 细灰边 |
| --text / --text-secondary / --text-muted | #fff / #888 / #444 | 白 / 亮灰 / 暗灰 |
| --accent | #fff | 主色 = 白（CYAN） |
| --danger | #ff3333 | 红 = RED_LINE |

> 语义完全一致；因 MC 为栅格渲染、无 CSS 盒阴影 / 字体渐变 / 反锯齿，取值取近似而非逐像素。

## 4. 关键组件实现

- **terminalPanel / card**：近黑底 + 灰描边。
- **selectedBar（选中态）**：**白底反白字** + 白左线，对齐前端 selected 反白；选中行文字须用 `RpTheme.ACCENT_TEXT`（深色）以保证白底可读。
- **RpButton**：`primary`=白底黑字 / `secondary`=暗底灰边白字 / `danger`=红边红字。
- **RpScrollbar**：游标用 CYAN（现为白）。
- **RpIcons.badge**：灰阶环（按 tier）+ 白色图形。
- **tag()**：`"[ 分类 ]"`。
- **scanlines**：每 3px 一条微亮白线（屏幕质感）。

## 5. 变更范围（本次已做）

- `RpTheme.java`：v3 → v4 调色板 + tierColor / statusColor / selectedBar / scanline。
- `RpButton.java` / `RpIcons.java` / `RpScrollbar.java`：经 `RpTheme.CYAN`→白 自动跟随。
- 清理各屏幕散落的**非中性色**（青 #45D8F2、蓝 #3D7BFF、金 #FFC84C、橙 #FF8C42、暗绿 #1F4D33、近黑蓝 #10181E/#15181E/#3A3F4A）→ 白 / 灰 / 红：`RpAdminScreen` / `RpRulesTab` / `RpRelationTab` / `CharacterManagementScreen` / `StatusHud` / `XpHudOverlay` / `DeployNoticeBanner` / `RecruitOverlayHud` / `FactionGraphScreen` / `PlayerNametagRenderer` / `ClientPacketHandlers`。
- 选中行文字白→深：各列表 `sel ? RpTheme.ACCENT_TEXT`。
- `README.md` / `RpTheme.java` Javadoc 同步为黑白灰军用终端描述。

### 5.1 终端容框完整视觉层（2.20.1，格式/细节/布局层）

> v4 只迁移了「调色板」，本层补齐前端 `App.css` 的**容器装饰**（非颜色，属格式/细节/布局）：

- **`RpTheme.terminalFrame()`**：素版面板（近黑底+灰描边）之上叠加**四角 L 型角标**（`cornerBrackets`，此前已定义但未调用）+ **顶部高光 rail**（低透明度，对齐 `.terminal-panel::before` 顶部渐变细线）。用于**顶层面板**（`CharacterManagementScreen` / `RpAdminScreen`）；内嵌弹窗/小卡片仍走 `terminalPanel`（无角标/网格）。
- **`RpTheme.gridOverlay()`**：面板细灰网格叠层（对齐 `.terminal-panel::after` 的 100px 网格，32px 步进、低透明度），呈「军用终端底格」质感。
- **CRT 扫描线**：`CharacterManagementScreen` 与 `RpAdminScreen` 在内容层之上叠加 `RpTheme.scanlines()`（对齐 `body::before`），此前仅 `FactionGraphScreen` 有。

### 5.2 K 面板 / 管理面板结构与标签对齐（2.20.2，结构/文案层）

> 在颜色 + 容器装饰之上，把**结构标签与文案**对齐参考 ccnr-rp-gui（`Terminal.jsx` / `DetailPanel.jsx` / `AdminPanel.jsx`），非仅改色：

- **顶部栏**：`[CCNR] TERMINAL` + `LV.x // STATUS: y` + `[ ADMIN ]` / `X`。
- **列头**：`[ NAV ]` / `[ POSITIONS ] [n]` / `[ DETAIL ]`；导航项 `[ ALL ]`、`[ 阵营名 ]` 统一方括号。
- **详情面板分区**：`ID: x // FACTION: y`、`[ LV.REQ: x | CURRENT: y ]`、`PERSONNEL: a/b // FACTION: c/d`、`// PREVIEW`、`EQUIPMENT`、`// PROFILE`。
- **部署按钮**：`[ DEPLOY ]` / `[ REDEPLOY ]` / `[ REQ LV.x ]` / `[ FULL occ/lim ]`。
- **管理面板**：页签 `[ 设置 ]` 方括号、标题 `[CCNR] ` 前缀。
- **标签语言策略**：结构标签用参考的**英文终端式**；内容（职业名/阵营名/画像文本）仍用本地化文本；zh_cn 与 en_us 键集一致（LangFileTest 保障）。

### 5.3 与前端一致性边界（重申）

- 前端为 CSS 渲染（渐变/box-shadow/抗锯齿），MC 为 int 色值、无渐变/阴影/抗锯齿，呈现为「同设计语言的近似值」，非逐像素相同。
- 扫描线/网格/角标均为**低透明度/细线**，不压过内容可读性；仅顶层主容器使用，内嵌弹窗保持素版。

### 5.4 全量样式整备（2.24.1，令牌收口 + 可读性修复）

> 在 v4 调色板 + 2.20.x 容器装饰之上，把「同一类构件在不同界面各写一套」的重复画法收敛为单一入口，并修掉可读性缺陷：

- **裸色值清零**：`com.ccnrcom.rp.client` 内样式裸色值全部替换为 §2.1 令牌（仅余 `#RRGGBB` 解析用的 alpha 掩码）。
  涉及：`RpButton` / `RpScrollbar` / `RpIcons` / `StatusHud` / `XpHudOverlay` / `RecruitOverlayHud` /
  `DeployNoticeBanner` / `KillFriendlyNoticeHud` / `EventBanner` / `FactionGraphScreen` / `RecruitPopupScreen` /
  `CharacterManagementScreen` / `RpAdminScreen` / `RpRulesTab` / `RpRelationTab` / `RpGroupsTab` / `RpExtensionTab` /
  `PlayerNametagRenderer` / `CinematicController` / `ClientPacketHandlers` / `RadioPlayer` / `ClientSetup`。
- **列表行统一**：此前 4 套画法（无悬停 / 白色条纹 / 整列同色 / 斑马纹）→ 统一 `listRow`（斑马纹 + 悬停 + 选中反白）；
  列表容器统一 `listPanel`，表头分隔线统一 `listHeaderRule`。
- **浮层统一**：HUD 与横幅此前有的无描边、底色三种深浅 → 统一 `hudCard`（近黑实心底 + 细灰边），
  警示类统一 `SURFACE_ALERT` 底 + 红系描边 + 红状态条；撤销「无边框卡片」这一历史不一致。
- **下拉 / 补全统一**：三处页签阵营下拉 + 四个补全浮层此前各写一份同构代码（底色/轨道/行高不同）
  → 统一 `controlBox` + `popupPanel`/`popupRow` + `suggestionPopup`/`suggestionRow`；候选行新增
  **键盘当前项反白**（`ACCENT_TEXT`）与**鼠标悬停高亮**，与列表选中态同一语言。
- **按钮统一**：`RpButton.draw` 增加悬停重载；经验规则页签「试算/保存/新增/删除/启停」与 K 面板确认框
  按钮改用同一配方（次级=控件底+细灰边+白字，主操作=白底+深色墨字）。
- **可读性修复**：K 面板部署确认「确认」按钮此前**白底白字不可读**（白底填充 + 白色文字）→ 主按钮配方；
  职业列表 `Lv x` / 在职上限标签改「亮底深字、红底白字」；`StatusHud` 进度条百分比文字改 `ACCENT_TEXT`。
- **语义一致**：击杀者名字的关系色此前为亮灰，与关系图/关系页签/入场电影的「友好=蓝」不一致
  → 四处统一走 `RpTheme.relationColor()`；关系页签与阵营组页签下拉底色的 v3 冷色残留（`0xF01B1E23`）、
  关系图节点遮罩（`0xA80E1014`）、经验 HUD 异色红（`0xFFFF4C4C`）、玩家名牌红（`0xFFFF5555`）一并归一。
- **边界**：只改视觉层；布局 / 几何 / 行为 / 配置 / 网络 / 存档 / 运行时逻辑未动（`g.fill` 的坐标、行高、
  命中判定均保持原值，含新增共享入口内部与之对齐的 12px 补全行高）。

### 5.5 K 面板构成主义硬边化（2.24.1，仅 `CharacterManagementScreen`）

> 前几轮只做了「令牌收口」——外观几乎不变，于是被反馈「看不出变动」。本节是一次**克制的视觉强化**，
> 范围严格限定在 K 面板（`CharacterManagementScreen`）一个文件内：把构成主义语言做实（硬边直角 / 45° 对角线 /
> 红为结构色），把科技感做在「刻度·序号·读数」上，而不是加光效。

- **删除柔光，改硬边**：设计语言里「不要多层 alpha 光晕」这条此前没被遵守——预览区阵营水印用
  `RpIcons.bigBadge(alpha=36)`（多层同心 alpha 圆）叠成一团灰斑；全息底座用宽幅 `fillGradient` + 多层同心圆。
  现改为：**水印 = 双层 1px 细环 + 两段红色断弧 + 四向刻度 + 中心十字 + 单扇区 45° 排线**；
  **底座 = 2px 核心竖线 + 虚线侧柱 + 硬环 + 八向刻度 + 汇聚透视线 + 基线刻度尺 + 红色断弧**。
- **红升级为结构色**：`STRUCT_RED`（0xFFE03028）只用于线条/序号块/楔形/刻度，与「危险实心红」
  （`RED_BG_FILL`：整块红底白字）在用法上区分——前者是构图，后者是告警。
- **科技感三件套**：① **刻度**（面板/卡片底缘刻度尺、导轨每槽刻度、底座八向刻度）；
  ② **序号**（小节头红底反白 `01/02/03`、装备条目前缀 `01..05`）；③ **读数框**（内陷底 + 左红条 + 右缘刻度 +
  左右两端对齐），把 `ID // FACTION` / `LV.REQ` / `PERSONNEL` 从裸灰字变成机器读数。
- **排版**：小节标题 = 全大写 + 字距放大 + 尾部细线（`sectionHead`）；职业名 = 字距放大白字 + 硬边标题线。
- **边界**：新原语全部为 `CharacterManagementScreen` 私有方法，**未改动 `RpTheme`/`RpIcons`**，故其它界面零影响；
  布局常量（卡片 80px、装备行距、部署按钮位置、各区矩形）全部未动；未增删任何语言包键；
  窄窗口下 `sectionHead`/`readout`/装备序号会自动省略或截断，不溢出面板。
- **如何失效**：若日后把水印/底座改回多层 alpha 同心圆，或在小节标题里塞渐变，本节的「硬边」前提即失效；
  新增这类装饰请沿用本节原语（或先把它们提升为共享原语再复用），不要各写一套。

## 6. 边界与注意（为什么 / 如何失效）

- **只改视觉**：布局 / 几何 / 行为 / 配置 / 网络 / 存档均不动，仅颜色与描边。
- **脱离主题的风险**：若新增界面直接写彩色字面量而不走 `RpTheme`，会与黑白灰终端不一致。
  2.24.1 起 `com.ccnrcom.rp.client` 内裸色值已清零，新增样式必须走 §2.1 令牌与共享入口；
  代码评审的检查方法：`grep -rn "0x[0-9A-Fa-f]\{8\}" src/main/java/com/ccnrcom/rp/client/ | grep -v RpTheme.java`
  应只剩 `#RRGGBB` 解析用的 alpha 掩码。
- **刻意保留的彩色（仅两处，均为语义刚需）**：
  1. **红色警戒 / 危险**（`RED`/`RED_LINE`/`RED_DIM`/`RED_BG*`）——唯一保留的警示色。
  2. **关系语义蓝**（`FRIENDLY` = 0xFF4FA6FF，配 `NEUTRAL` 白 / `HOSTILE` 红）——关系图、关系页签、
     入场电影与击杀播报需要「友好/中立/敌对」三态可区分，红已被敌对占用，故保留蓝作为第三色；
     三处统一走 `RpTheme.relationColor()`，不得各自另取颜色（2.24.1 前击杀播报的「友好」曾用亮灰，属偏差，已修）。
  3. 另有 `STATUS_OBSERVING`（0xFF7E8A8F，观察态弱灰-青）为状态语义刻意保留的弱色。
- **白色选中可读性（硬约束）**：反白依赖 `selectedBar` 白色底 + 该行文字用 `ACCENT_TEXT`（深色）；
  任何「亮底/白底」填充（`selectedBar`、`ACCENT_FILL`、`popupRow(current=true)`、亮灰标签块）之上的文字
  **必须**用 `ACCENT_TEXT`，否则白底白字不可读。2.24.1 已修：K 面板部署确认「确认」按钮、职业列表
  `Lv x` / 在职上限标签、`StatusHud` 进度条百分比。
  新增同类构件时按此三条自检：①该填充是否为亮色；②文字是否深色；③深色文字是否只在亮底分支出现。
- **与前端一致性边界**：前端为 CSS 渲染，MC 为 int 色值、无渐变/阴影/抗锯齿，呈现为「同设计语言的近似值」，非逐像素相同。
- **颜色守护（2.24.1 起）**：`RpThemeTest` 把本节的契约变成可执行断言——①反射遍历 `RpTheme` 全部颜色令牌，
  凡非中性灰（R≠G≠B）必须在「刻意保留清单」内；②反白填充 + `ACCENT_TEXT` 的对比度 ≥ 4.5（WCAG AA）；
  ③浮层卡片比面板更不透明、内陷槽比卡片底更亮、列表悬停比常态亮、滚动条轨道按溢出分档；
  ④等级灰阶单调、关系三态两两不同且未知值回退中立；⑤`alphaBlend` 保 RGB 只改 alpha。
  新增/改色令牌若违反契约，`build -PrunTests` 直接红。渲染结果本身仍靠人工 review（无快照测试）。
- 一致性维护流程：改色后跑 `spotlessApply` + `build -PrunTests`（后者含颜色守护），并同步 §2.1 令牌表。

## 7. 验收清单（DoD）

- [x] `spotlessApply` / `build` / `test -PrunTests` 全绿。
- [x] 设计系统（RpTheme）+ 按钮 + 图标 + 各屏幕非中性色清理。
- [x] 选中态反白（selectedBar + ACCENT_TEXT）。
- [x] README / RpTheme Javadoc 同步。
- [x] 版本号升至 **2.20.0** + CHANGELOG 置顶条目 + docs/14。
- [x] 2.20.1 终端容框完整视觉层：`terminalFrame`（角标+顶部 rail）+ `gridOverlay` + 顶层屏幕 CRT 扫描线；`spotlessApply` / `build` / `test -PrunTests` 全绿。
- [x] 2.20.2 K 面板 / 管理面板结构与标签对齐参考界面（终端英文标签 + 分区布局）；`spotlessApply` / `build` / `test -PrunTests` / `LangFileTest` 全绿。
- [x] 2.20.3 恢复项目图标（`icon.png`）并修正 JAR 元数据描述（`logoFile` + 真实项目简介），对齐前端/文档一致的品牌展示；`ProjectMetadataTest` 加固（logoFile + 图标 PNG 断言）全绿。
- [x] 2.21.0 去 Emoji + 国际化（客户端 UI 全部走语言包，zh_cn/en_us 同步）+ 阵营组编辑页签 + 入场电影 per-faction 可配置（全屏黑开关 / 简洁模式）；`spotlessApply` / `build` / `test -PrunTests` / `LangFileTest` 全绿。
- [x] 2.24.0 阵营页签内的属性编辑器弹窗（docs/01 §10 已登记）：颜色/描边/选中态一律走 `RpTheme`/`RpButton`/`RpRoundRect`，结构标签沿用终端英文方括号风格；`spotlessApply` / `build` / `test -PrunTests` / `LangFileTest` 全绿。（同版新增的「拓展设定」页签已在 2.25.0 删除，见下条。）
- [x] 2.24.1 全量 GUI 样式整备：令牌全表（§2.1）登记完毕、`client` 包样式裸色值清零、列表/浮层/下拉/补全/按钮各只有一种画法（共享入口见 §2.1 下半表）；修掉白底白字（K 面板确认框 / 亮底标签 / HUD 进度百分比）与关系语义色不同源（击杀播报 vs 关系图）；README / CHANGELOG / docs/14 同步；`spotlessApply` / `build` / `test -PrunTests` 全绿。
- [x] 2.24.1 新增颜色守护 `RpThemeTest`（10 个用例：令牌中性化 / 反白对比度 / 明度阶梯 / 等级灰阶 / 关系三态 / 状态回退 / 列表行斑马纹 / alphaBlend / 标签格式），`test -PrunTests` 195 用例 0 失败。
- [x] 2.24.1 K 面板（K 键终端）构成主义硬边化：水印/底座去柔光改硬边几何、档案卡改读数框、小节头带红色序号、装备轨加刻度与条目编号；仅改 `CharacterManagementScreen` 一个文件（新原语为文件内私有方法，未动共享主题），布局与语言包零改动；`spotlessApply` / `build` / `test -PrunTests` 全绿。
- [x] 2.25.1 全管理面板删除交互统一为「右键条目 + 二次确认」（规范沉淀在 docs/01 §10.2）：确认框为自绘模态，打开期间吞掉点击/按键/字符输入；删除按钮与右键走同一入口。
- [x] 2.25.1「自定义设定」页签的删除交互改为**右键条目 + 二次确认**（自绘模态：隐藏下层 + 吞掉全部点击与按键 + Esc 取消；主按钮白底反色 + `ACCENT_TEXT`），并给预设值/方案胶囊加勾选框支持多选批量删（docs/01 §10 弹窗三条件）。
- [x] 2.25.0 新增「自定义设定」页签（`RpVariablesTab`，替换 2.24.0 的「拓展设定」页签）：左侧列表 + 右侧编辑器，编辑器用「变量 / 预设值 / 方案」三个子页签分段承载全部字段（面板最小高度 340px，纵向堆叠会溢出可视区）；预设值与方案做成**胶囊**，悬停即白底反白（文字用 `ACCENT_TEXT`，遵守 §2.1 可读性硬约束），点击直接切换/套用；变量类型用循环按钮而非下拉（三个取值，省一层浮层）。同版删除区域与阵营弹头许可，阵营页签入场电影那行由三连开关退回两连开关（`bw2` 由 `(w-8)/3` 改 `(w-4)/2`），布局与语言包里 `gui.admin.area.*` / `gui.admin.warhead.*` / `tab.extension` 键一并移除；`spotlessApply` / `build` / `test -PrunTests` 全绿。

## 8. 项目品牌与 JAR 元数据（2.20.3）

> 界面的「黑白灰军用终端」之上，项目身份与 JAR 卡牌展示保持与前端 / 文档一致（docs/01 §9.1 文档-代码-实际一致）：

- **图标**：`src/main/resources/icon.png`（128×128 RGBA）——CCNR-Com 笑脸 + 右下角蓝色「RP」标。此前在 `ec391ae`（jar 瘦身）被移除，本次从 git 历史 `46f9c01` 还原。
- **mods.toml**：`logoFile="icon.png"`；`description` 为真实项目简介（量子科学设施 CCNR 机构、八大系统、终端三栏界面）。
- **如何失效 / 校验**：若未来再次移除 `icon.png` 或把 `description` 改回「骨架/开发中」占位，`ProjectMetadataTest.modIconBundled` 与 `modsTomlDeclaresOptionalCorpseDependency`（含 `logoFile` + 非过期描述断言）会失败，防止品牌与文档脱节。

